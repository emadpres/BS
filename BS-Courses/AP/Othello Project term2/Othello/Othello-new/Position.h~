#include "OurBasics.h"
#include "Basics.h"
class Position
{	
	public:
		int makeMoveList(MOVE list[]);
		void doMove(MOVE * move);
		void undoMove(MOVE * move);
		
		int board[GAMESIZE][GAMESIZE];
		bool turn;
};
